@Library("shared-library") _
pipeline{
    agent node1
    stages{
        stage('Hello') {
            steps{
               sh echo 'Hello world!'
            //    addSidebarLink(url:'') 
            }
        }
    }
}