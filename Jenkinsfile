@Library("shared-library") _
pipeline{
    node(node1){
        stages{
           stage('Hello') {
                steps{
                   sh echo 'Hello world!'
                //    addSidebarLink(url:'') 
                }
            }
        }
    }
}